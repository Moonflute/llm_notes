import Link from "next/link"; import {conceptDocuments} from "@/lib/content"; export default function Concepts(){return <main id="main-content" className="directory"><p className="sectionLabel">HOW IT WORKS</p><h1>개념</h1><p className="intro">용어의 정의에서 작동 원리, 다음 학습 주제까지 차례로 연결합니다.</p><div className="directoryGrid">{conceptDocuments.map(c=><Link className="directoryCard" href={`/concepts/${c.slug}/`} key={c.slug}><span>{c.level}</span><h2>{c.titleKo}</h2><p className="en">{c.titleEn}</p><p>{c.summary}</p><small>선수 지식 {c.prerequisites.length}개 →</small></Link>)}</div></main>}