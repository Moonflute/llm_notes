import type {Metadata} from "next";
import Link from "next/link"; import {ContentMeta} from "@/components/content/content-meta";
import {conceptDocuments,getSource,modelFamilies,modelReleases} from "@/lib/content";
import {notFound} from "next/navigation";
export function generateStaticParams(){return modelReleases.map(item=>({family:item.familySlug,release:item.slug}))}
export async function generateMetadata({params}:{params:Promise<{family:string;release:string}>}):Promise<Metadata>{const {family,release}=await params;const item=modelReleases.find(entry=>entry.familySlug===family&&entry.slug===release);return {title:item?.title??"모델 릴리스",description:item?.summary,alternates:{canonical:`/models/${family}/${release}/`}}}
export default async function Release({params}:{params:Promise<{family:string;release:string}>}){const {family,release}=await params;const modelFamily=modelFamilies.find(item=>item.slug===family);const item=modelReleases.find(entry=>entry.familySlug===family&&entry.slug===release);if(!modelFamily||!item)notFound();const familyReleases=modelReleases.filter(entry=>entry.familySlug===family).sort((a,b)=>a.date.localeCompare(b.date));const position=familyReleases.findIndex(entry=>entry.slug===release);const previous=familyReleases[position-1];const next=familyReleases[position+1];return <main id="main-content" className="detail"><Link href={`/models/${family}/`} className="back">← {modelFamily.titleKo} 계열</Link><p className="sectionLabel">MODEL RELEASE · {item.date}</p><h1>{item.title}</h1><p className="detailEn">{item.titleEn}</p><p className="lead">{item.summary}</p><ContentMeta/><section><h2>계열 내 비교</h2><div className="releaseNav">{previous?<Link className="textLink" href={`/models/${family}/${previous.slug}/`}>← 이전 · {previous.title}</Link>:<span>이전 릴리스 없음</span>}{next?<Link className="textLink" href={`/models/${family}/${next.slug}/`}>다음 · {next.title} →</Link>:<span>다음 릴리스 없음</span>}</div></section><section><h2>핵심 사양</h2><p>입력·출력 모달리티, 컨텍스트 길이, 파라미터 수 등 공개된 사양만 기록합니다. 이 릴리스의 비공개 항목은 <b>미공개</b>입니다.</p></section><section><h2>관련 개념</h2><div className="tags">{item.conceptIds.map(id=>{const concept=conceptDocuments.find(entry=>entry.slug===id);return concept?<Link href={`/concepts/${id}/`} key={id}>#{concept.titleKo}</Link>:null})}</div></section><section><h2>공식 자료</h2>{item.sourceIds.map(id=>{const source=getSource(id);return source?<a className="source" href={source.url} key={id} target="_blank" rel="noreferrer"><span>Tier {source.tier??1} · {source.publisher}</span><b>{source.title} ↗</b><small>{source.year} · 최종 검증 {source.verifiedAt??"미확인"}</small></a>:null})}</section></main>}