import Link from "next/link"; import {modelFamilies,organizationDocuments} from "@/lib/content"; export default function Models(){return <main id="main-content" className="directory"><p className="sectionLabel">MODEL FAMILIES</p><h1>모델과 계보</h1><p className="intro">제품명과 기반 모델을 분리하고, 계열의 변화를 출처와 함께 기록합니다.</p><div className="directoryGrid">{modelFamilies.map(m=>{const org=organizationDocuments.find(o=>o.slug===m.organizationSlug);return <Link className="directoryCard" href={`/models/${m.slug}/`} key={m.slug}><span>{org?.titleKo}</span><h2>{m.titleKo}</h2><p className="en">{m.titleEn}</p><p>{m.summary}</p><small>{m.releaseSlugs.length}개 샘플 릴리스 →</small></Link>})}</div></main>}