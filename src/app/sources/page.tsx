import {sources} from "@/lib/content";export default function Sources(){return <main id="main-content" className="directory"><p className="sectionLabel">SOURCE LIBRARY</p><h1>출처</h1><p className="intro">공개 콘텐츠에서 날짜·사양·주장을 뒷받침하는 원자료 목록입니다.</p><div className="results">{sources.map(s=><a className="result" href={s.url} key={s.id} target="_blank" rel="noreferrer"><span>Tier {s.tier??1} · {s.publisher} · {s.year}</span><h2>{s.title} ↗</h2></a>)}</div></main>}